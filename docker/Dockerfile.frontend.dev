# Frontend Dockerfile для разработки
FROM node:22-alpine

# Рабочая директория
WORKDIR /app

# Копируем package.json
COPY frontend/package*.json ./

# Установка зависимостей
RUN npm ci

# Копируем код приложения
COPY frontend/ ./

# Порт
EXPOSE 5173

# Запуск dev сервера
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
